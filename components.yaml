repos:
  local_repo:
    type: local
    path: .

components:
  email_agent:
    repo: local_repo
    file_path: entrypoint.py
    entry_point: compose_email     
    requirements_path: requirements.txt
    use_venv: false
    input_parameters:
      bullets:
        type: string
      sender_name:
        type: string

